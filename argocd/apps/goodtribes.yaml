apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: goodtribes
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:goodtribes-org/deploy.git
    targetRevision: HEAD
    path: goodtribes
  destination:
    server: https://kubernetes.default.svc
    namespace: goodtribes
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
